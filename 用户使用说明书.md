# 大学生新生智能报道助手 - 用户使用说明书（API版）

**版本**: V2.0  
**日期**: 2026-04-25  
**适用对象**: 开发者/管理员  

---

## 一、 系统简介

基于Django + PostgreSQL + DRF的企业级新生报到系统，提供RESTful API接口。

---

## 二、 快速开始

### 2.1 环境要求
- Python 3.10+
- PostgreSQL 14+
- 内存: 2GB+
- 磁盘: 500MB+

### 2.2 安装步骤

**Step 1: 安装PostgreSQL**
1. 下载: https://www.postgresql.org/download/windows/
2. 安装时记住设置的postgres密码
3. 安装pgAdmin（可选）

**Step 2: 创建数据库**
`ash
createdb -U postgres freshman_db
`

**Step 3: 安装依赖**
`ash
cd D:\freshman-assistant\gxt-freshman
pip install -r requirements.txt
`

**Step 4: 配置环境变量**
创建 .env 文件：
`
SECRET_KEY=django-insecure-your-secret-key
DEBUG=True
DB_NAME=freshman_db
DB_USER=postgres
DB_PASSWORD=你的PostgreSQL密码
DB_HOST=localhost
DB_PORT=5432
`

**Step 5: 数据库迁移**
`ash
python manage.py migrate
python manage.py createsuperuser
`

**Step 6: 启动服务**
`ash
python manage.py runserver
`

---

## 三、 API接口使用

### 3.1 查看API文档

访问: http://localhost:8000/swagger/

功能：
- 查看所有API接口
- 在线测试接口
- 查看请求/响应示例

---

### 3.2 认证接口

#### 登录获取Token

**请求**:
`ash
POST http://localhost:8000/api/auth/login/
Content-Type: application/json

{
  \"username\": \"admin\",
  \"password\": \"admin123\"
}
`

**响应**:
`json
{
  \"access\": \"eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...\",
  \"refresh\": \"eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...\",
  \"expires_in\": 7200
}
`

#### 刷新Token

**请求**:
`ash
POST http://localhost:8000/api/auth/refresh/
Content-Type: application/json

{
  \"refresh\": \"eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...\"
}
`

---

### 3.3 学生报到接口

#### 获取学生档案

**请求**:
`ash
GET http://localhost:8000/api/students/profile/
Authorization: Bearer <access_token>
`

**响应**:
`json
{
  \"id\": 1,
  \"user\": {
    \"id\": 1,
    \"username\": \"2024001\",
    \"email\": \"\"
  },
  \"student_id\": \"2024001\",
  \"real_name\": \"\",
  \"phone\": \"\",
  \"dorm_info\": \"\",
  \"status\": \"pending\",
  \"report_time\": null
}
`

#### 提交报到信息

**请求**:
`ash
PATCH http://localhost:8000/api/students/report/
Authorization: Bearer <access_token>
Content-Type: application/json

{
  \"real_name\": \"张三\",
  \"phone\": \"13800138000\",
  \"dorm_info\": \"1号楼302\"
}
`

**响应**:
`json
{
  \"id\": 1,
  \"student_id\": \"2024001\",
  \"real_name\": \"张三\",
  \"phone\": \"13800138000\",
  \"dorm_info\": \"1号楼302\",
  \"status\": \"done\",
  \"report_time\": \"2026-04-25T10:30:00Z\"
}
`

---

## 四、 Admin后台使用

### 4.1 访问后台
地址: http://localhost:8000/admin/

### 4.2 登录
使用createsuperuser创建的账号登录

### 4.3 功能
- 用户管理（增删改查）
- 学生档案管理
- 报到状态筛选
- 数据导出

---

## 五、 常见操作

### 5.1 创建测试用户

**方法1: Admin后台**
1. 登录Admin
2. Users  Add user
3. 填写用户名和密码
4. 自动创建学生档案

**方法2: Django Shell**
`ash
python manage.py shell
`
`python
from django.contrib.auth.models import User
user = User.objects.create_user('2024002', password='123456')
user.save()
`

### 5.2 查看报到统计

**SQL查询**:
`sql
SELECT status, COUNT(*) 
FROM student_profiles 
GROUP BY status;
`

**Django Shell**:
`python
from apps.students.models import StudentProfile
StudentProfile.objects.values('status').annotate(count=Count('id'))
`

---

## 六、 故障排查

### Q1: 数据库连接失败？
**错误**: could not connect to server

**解决**:
1. 检查PostgreSQL服务是否启动
2. 检查.env中的DB_PASSWORD
3. 测试连接: psql -U postgres -d freshman_db

### Q2: 迁移失败？
**错误**: django.db.utils.ProgrammingError

**解决**:
`ash
# 删除数据库重新创建
dropdb freshman_db
createdb freshman_db
python manage.py migrate
`

### Q3: Token过期？
**错误**: 401 Unauthorized

**解决**:
使用refresh接口获取新Token

### Q4: CORS错误？
**错误**: Access-Control-Allow-Origin

**解决**:
在.env中添加前端域名到CORS_ALLOWED_ORIGINS

---

## 七、 开发工具推荐

### 7.1 API测试
- Postman
- Insomnia
- Swagger UI（内置）

### 7.2 数据库管理
- pgAdmin
- DBeaver
- DataGrip

### 7.3 IDE
- PyCharm
- VS Code
- Cursor

---

## 八、 性能优化建议

### 8.1 数据库
- 添加索引: student_id, status
- 使用连接池: PgBouncer
- 定期VACUUM

### 8.2 缓存
- Redis缓存Token
- 缓存查询结果

### 8.3 部署
- 使用Gunicorn
- Nginx反向代理
- 启用HTTPS

---

**文档版本**: V2.0  
**更新日期**: 2026-04-25  
**开发者**: gxt11
